@model IList<GroupWebApp.Storage.Entities.Category>

    <h1>Категории</h1>


@foreach (var category in Model){
    <a asp-controller="SubCategories" asp-route-id="@category.Id" class="category_button">@category.categoryName</a> 
}